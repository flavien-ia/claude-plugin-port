{
  "description": "Demo guard: refuses `rm -rf /`, asks before `git push`.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash|Monitor",
        "hooks": [
          { "type": "command", "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/guard.mjs\"", "timeout": 10 }
        ]
      }
    ]
  }
}
